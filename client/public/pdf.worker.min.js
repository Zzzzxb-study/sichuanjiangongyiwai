/*! pdf.js worker dummy file */
// This is a minimal dummy worker file to satisfy PDF.js requirements
// The actual PDF processing is handled by the main thread using legacy build

try {
  // Import the legacy worker from pdfjs-dist
  importScripts('https://cdnjs.cloudflare.com/ajax/libs/pdf.js/5.4.530/pdf.worker.min.mjs');
} catch (e) {
  console.warn('PDF.js worker loading failed, but this is expected for legacy build:', e);
}
